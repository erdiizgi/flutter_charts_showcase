// holds change in percentage info for a crypto asset on a specific date
class CryptoPercentageChange {
  final double percentageChange;
  final DateTime date;

  const CryptoPercentageChange(this.percentageChange, this.date);
}
